---
galaxy_info:
  role_name: ispconfig3
  author: Michael Kunze, Martin Lesser, Karl-Erik Kley
  description: Installs and customizes ISPConfig 3 on Ubuntu Bionic
  company: Inoxio Quality Services GmbH

  license: Apache 2.0

  min_ansible_version: 2.6.4

  platforms:
    - name: Ubuntu
      versions:
        - all

  galaxy_tags:
    - inoxio
    - ubuntu
    - bionic
    - installation
    - setup
    - unattended
    - automatic
    - molecule
    - custom
    - customization
    - ispconfig

dependencies:
  - role: geerlingguy.certbot
    certbot_admin_email: "{{ certbot.admin_email }}"
    certbot_create_standalone_stop_services:
      - "{{ certbot.create_standalone_stop_services | default(certbot_create_standalone_stop_services) }}"
    certbot_install_from_source: "{{ certbot.install_from_source | default(certbot_install_from_source) }}"
